# ============================================
# AI Arena - Environment Variables Reference
# ============================================
# Copy this file and rename to .env
# Fill in the values from your respective dashboards
#
# NOTE: VITE_SUPABASE_* variables are auto-configured 
# by Lovable Cloud and should NOT be changed manually.
# ============================================

# --- Supabase (Auto-configured by Lovable Cloud) ---
VITE_SUPABASE_PROJECT_ID="your-supabase-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-anon-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# --- Razorpay Payment Gateway ---
# Get these from: https://dashboard.razorpay.com/app/keys
# These are stored as Lovable Cloud secrets (edge function env vars), NOT in .env
#
# RAZORPAY_KEY_ID=rzp_test_XXXXXXXXXX        # Razorpay Key ID (publishable, used in frontend too)
# RAZORPAY_KEY_SECRET=XXXXXXXXXXXXXXXX        # Razorpay Key Secret (private, edge functions only)
# RAZORPAY_WEBHOOK_SECRET=XXXXXXXXXXXXXXXX    # Webhook secret for signature verification

# --- Razorpay Frontend Key (safe to expose) ---
# This is the publishable key used by the Razorpay checkout widget on the frontend.
# Set this in your .env file:
VITE_RAZORPAY_KEY_ID="rzp_test_PLACEHOLDER"

# ============================================
# SECRETS STORED IN LOVABLE CLOUD (Edge Functions)
# ============================================
# The following secrets must be added via Lovable Cloud > Secrets:
#
# 1. RAZORPAY_KEY_ID        - Your Razorpay Key ID
# 2. RAZORPAY_KEY_SECRET    - Your Razorpay Key Secret  
# 3. RAZORPAY_WEBHOOK_SECRET - Webhook signature verification secret
#
# These are available as Deno.env.get('SECRET_NAME') in edge functions.
# ============================================
