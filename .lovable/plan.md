## Complexity Assessment: **Easy to Normal**

This is a straightforward find-and-replace task across ~27 files with ~350 total occurrences. No logic changes, no database changes, no API changes. Just text/string replacements.

---

## Rename Plan: "CompareAI / AI Arena" → "DML Arena"

### What Changes

**27 files** need updates across these categories:

#### 1. Display Names & Branding (user-visible text)

- `AI Arena` → `DML Arena`
- `CompareAI` → `DML Arena`
- `Compare AI` → `DML Arena` (in descriptions)
- `gradient-text` headings, badges, footer text

**Files:** `Landing.tsx`, `Header.tsx`, `index.html`, `manifest.json`, `PRD.md`, `env.example`, pricing descriptions in `razorpay-create-order/index.ts`

#### 2. Internal Storage Keys (localStorage prefixes)

- `compareai-` → `dmlarena-`

**Files:** `useSettings.ts`, `useOfflineCache.ts`, `useModelPresets.ts`, `usePinnedResponses.ts`, `useTour.ts`, `ThemeProvider.tsx`, `ApiKeysSettings.tsx`, `models.ts`

#### 3. Export Filenames & Content

- `compareai-history-*.json` → `dmlarena-history-*.json`
- `Generated by CompareAI` → `Generated by DML Arena`
- `# CompareAI History Export` → `# DML Arena History Export`

**Files:** `BulkExport.tsx`, `exportUtils.ts`, `Settings.tsx`

#### 4. Meta Tags & SEO

- Page titles, OG tags, Twitter cards, manifest name/short_name

**Files:** `index.html`, `manifest.json`

#### 5. Edge Function References

- `X-Title: CompareAI` → `X-Title: DML Arena`
- `HTTP-Referer` URL reference

**Files:** `compare-ai-stream/index.ts`, `compare-ai/index.ts`

#### 6. Tour & Onboarding

- `Welcome to CompareAI` → `Welcome to DML Arena`

**Files:** `useTour.ts`

#### 7. Project Memory

- Update the `project-identity` knowledge to reflect "DML Arena"

### What Does NOT Change

- Edge function folder names (`compare-ai-stream/`, `compare-ai/`) — these are API endpoints and renaming them would break the integration
- `useStreamingComparison.ts` endpoint URL — must match the function name
- Database table/column names

---

User Edit

---

- What about Creating a New Edge Function + Db + etc ?? 
- Everything is Same Except the Name ??
- 99.99% Same Old Things but the only change is Name to DML Arena 